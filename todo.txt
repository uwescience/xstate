1. Handle feature combinations via "--"
     a) generalize isCompatible
     b) predict must consider multiple possible predictions
2. Prediction must have option to consider only those feature vectors
   that exist in the training data.
